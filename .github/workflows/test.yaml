name: Tests

on:
  pull_request:

jobs:
  unit-tests:
    uses: canonical/operator-workflows/.github/workflows/test.yaml@test-netbox
    secrets: inherit
    with:
      shellcheck-working-directory: "./charm"
      docs-working-directory: "./charm"
